# FlashEditor

A C# WinForms desktop application for editing animations and sprites with OpenGL rendering.

## Tech Stack

- C# / .NET
- WinForms UI
- OpenGL (texture caching, model rendering)

## Getting Started

Open `FlashEditor.sln` in Visual Studio and build.
