# Documentation conflicts and corrections

A running log of claims in `CLAUDE.md`, `AGENTS.md`, `STATE_OF_THE_EDITOR.md` and `reference/`
that turned out to be wrong, ambiguous, or true only of one particular cache.

Two rules this log exists to enforce:

1. **Record architecture, not statistics.** How a container is laid out, how a group is
   addressed, which field decides a payload width - these do not change. How many item
   definitions exist changes with the cache in front of you, and a number written down is read
   later as a target.
2. **A claim in a document is a claim, not evidence.** Where a document and the data disagree,
   the data wins, and the correction gets written back here so the next reader does not pay for
   it again.

## Open

| Where | Claim | Problem |
|---|---|---|
| `STATE_OF_THE_EDITOR.md` | Assessment header dated 2026-07-31; sections 1, 2 and 7 describe a build and suite that no longer exist | Already flagged in `CLAUDE.md`, still unresolved. Sections 7a-7f are worth keeping; the rest is a historical write-up presented as current fact. |
| `index-survey/00-WORKLIST.md` §4.2, repeated by me in a build brief | Index 21 opcode 2 is two u16s | It is one u16 (`Class107.java:255`). The two-u16 shape is index **20**'s opcode 20 (u8 slot, u16, u16); index 20's opcode 19 is u8, u8. The claim was being used as justification for the shared opcode record, which stands on other grounds. |
| `index-survey/index-020-ANIMATIONS.md` | Opcode 1's payload is 1 + 6n bytes | 2 + 6n. The count is a u16. |
| `index-survey/index-021-GRAPHICS.md` | Handles 16 opcodes; opcode 10's flag has no reader anywhere | The client handles 17. The flag has two readers outside the decoder, `Class333.java:65` and `Class340.java:80`, both gating whether movement can cancel or defer the graphic's animation. |
| `index-survey/index-012-CLIENT-SCRIPTS.md` | Names the four footer count fields | Two pairs are the wrong way round. `Class247.java:7881-7893` sizes the callee's arrays by one field and copies only the other off the caller's stack, so fields 1 and 2 are **local-variable** counts and 3 and 4 are **parameter** counts. The data agrees: parameters never exceed locals in any of the 4,149 scripts in either cache. Settled from usage, never from the decompiled names. |
| `index-survey/00-WORKLIST.md`, and my own framing of it | Index 8 gates indexes 13 and 32, both holding sprite-format payloads | Half right, for the wrong reason. Index 13 is **not** sprite format - it holds `Class197` font metrics read by `Class119_Sub1.method2182`, and none of its 25 groups parses as a sprite set. But the index-8 dependency is real as a **join**: `Class114.java:82,89` passes one id to both archives, and all 25 index-13 ids exist in index 8 with byte-identical name hashes, so a glyph sheet pairs to its metrics by id. Index 32 is fed by the sprite decoder but is **mixed**: 5 of 26 groups are 256-frame glyph sheets, 21 are JPEG ending `FF D9`. `InterfaceSettings.java:73-74` picks between index 32 and 34 for that role. |
| `Texture.cs:522-526` | Comment says two type-12 opcodes are swallowed | There are four: `(12,2)`, `(12,4)`, `(12,5)`, `(12,6)`. Both type-12 nodes in the cache are affected. |
| `TextureGraphEvaluator.cs:1660` | `EvalBlur`, headed "TYPE 17: Blur" | Dead code, and the same file's live type-17 arm is headed "NOT blur". The client settles it as an HSL adjust. Three further mono arms (types 10, 20, 33) are unreachable by the same mono-flag rule. Type 21 is a three-input lerp with no numeric parameter, not an emboss, and both files say emboss. |
| `Texture.cs` decoder comments | Type labels for 9, 22, 30 and 35 | Wrong; 9 and 22 are labelled as each other's job. The evaluator has all four right, so the two files disagree with each other. This is the trap `CLAUDE.md` already records - settle a type from the dispatch and the client, never from a method's own header. |
| `index-survey/index-009-TEXTURES.md` | 946 groups, table version 443, 3784 trailing bytes, 507 uncompressed, index 26 declaring 1408 textures | All repack residue. Build 639 is 915, 440, 0, 476, and index 26 declaring 915 - index 26 and index 9 are 1:1 in the vanilla capture. |
| `TextureGraphConformanceTests.EveryTexture_ProducesAThumbnail` | Iterates textures declared by index 26 but carrying no graph in index 9 | The repack has 462 of those; the vanilla capture has none, so on the default cache that loop body never runs. The test is not silently green - it reports the population - but the branch needs either a synthetic case or an explicit statement that it only exercises on the repack. |
| `RealCacheMidiPatchTests.TheMidiPatchBank_HoldsWhatTheCodecClaimsItDoes`, and `TODO.md` item 23 which repeats it | "the ten ids from 128 up are the GM drum kits at their canonical program offsets - standard, room, power, electronic, TR-808, jazz, brush, orchestra and SFX" | Nine names for ten ids, and one of the nine is not in this cache. GS puts its kits at drum-bank program offsets 0, 1, 8, 16, 24, 25, 32, 40, 48 and 56, which are ids 128, 129, 136, 144, 152, 153, **160**, 168, 176 and 184. The id list the same test asserts holds 178 and no 160, so there is no Jazz kit and there is one kit at an offset no published table names. Settled from the test's own asserted id array against the published GS kit offsets, so it needs no cache to see. `GeneralMidi` labels 178 by its bank and program rather than inventing a name, and `GeneralMidiTests` pins both halves. |

## Corrected

| Where | Was | Is | How it was settled |
|---|---|---|---|
| `hydra-model-decoding/MODEL_DECODING_ANALYSIS.md`, `index-survey/index-007-MODELS.md` | Signed smart's two-byte range is -49152..16383 | -16384..16383 | The reader only takes that branch when the leading byte has bit 7 set, so the biased u16 cannot fall below 0x8000. Read off `JagStream.ReadSmart`. |
| `index-survey/index-000-FRAMES.md` | Two-byte smart's first byte is 0xBF..0xC0 | 0x80..0xFF | Same branch condition. |
| `index-survey/index-000-FRAMES.md` | 1568 empty frames | 1573 | A sector-chain sweep that decodes no frames at all. |
| `index-survey/00-WORKLIST.md` §4.3 | Indexes 4 and 12 hold groups absent from their reference table | Indexes 3, 4, 12 and 32 do | `RealCacheEnumerationTests`, which failed on its first run against the documented claim. |
| `index-architect-02.md` | 27 damage-mark records carry the bare `%1` substitution | 26 do: one record has no template opcode, one stores the empty string | A raw opcode walk transcribed from the client, going through neither the document nor our decoder. |
| `index-survey/00-WORKLIST.md` §1, every row | A per-index "Capability now" grading, e.g. "3 INTERFACE_DEFINITIONS \| none (empty TabPage)", "2 CONFIG \| partial-read" | Stale on every row. Section 1 is now banner-marked historical; sections 0, 2, 4 and 5 stay live | The code. Every index holding content decodes, encodes and re-encodes byte-identically, and the survey predates all of it. This is a survey-document failure mode rather than a single wrong fact: prose is written once and never re-measured. |
| `index-survey/index-002-CONFIG.md` | 3 of 35 groups decode, "GUI: none" | 35 of 35, with a tab. 16 modelled families plus 19 providerless groups on `EmptyConfigDefinition` | Counted off `ConfigFamily.Modelled` (`ConfigFamily.cs:376`) and `ConfigGroup.EmptyProviderless`, and the Config tab observed in the running application. Superseded by `index-architect-02.md`. |
| `index-survey/index-003-INTERFACE-DEFINITIONS.md` | "Nothing index-3 specific exists", the TabPage is empty, no definition class, no `unpackConfig` anywhere | A component codec, a populated Interfaces tab and a whole-index byte-identity sweep all exist. Its "Gaps" list is a list of finished work | `FlashEditor/Definitions/Interfaces/` and the Interfaces tab observed in the running application. Superseded by `index-architect-03.md`. |
| `index-survey/index-012-CLIENT-SCRIPTS.md` | "Capability: none" | Codec, whole-index sweep, tab and a disassembler that resolves jump targets | The Client Scripts tab observed in the running application. |
| `CLAUDE.md` UI conventions, and `TODO.md` item 27's own correction of it | "25 pages in six categories" | The `TabControl` holds **27** `TabPage`s; **25** are reachable from the category `TreeView`. `Reference Tables` and `Containers` are in the strip and not in the tree | UI Automation enumerated all 27 `TabItem`s while the tree, captured in the same screenshot, showed 25. A count taken from the tree and a count taken from the strip are different numbers and the document did not say which it meant. |
| `CLAUDE.md` UI conventions, layout rule | Implies the form scales, by prescribing `AutoSize` against a bad scale factor | Nothing in this application scales. The process is pinned **DPI-unaware** (`FlashEditorForm.cs:46`), a crash fix for OpenTK changing the awareness context mid-session, so `AutoScaleMode.Dpi` against `(96, 96)` is exactly 1.0 everywhere | Read off `FlashEditorForm.cs:12-46`. The prescription is still right - it is what protects against the next factor change - but the inference that a vector asset resists high-DPI blur is wrong: Windows bitmap-stretches the whole window after it is painted. |
| `TODO.md` item 18, the paragraph scoping the work | "exactly two `ToolTip` instances"; "no icon resources"; the docked-label list including `MapEditorPanel.cs:1613` | **One** `ToolTip` instance attached to two controls, one of which the list missed, plus a `ToolTipText` **property** on a menu item, which is a different mechanism. One icon resource exists (`Flash.ico`, both `<ApplicationIcon>` and `$this.Icon`). `MapEditorPanel.cs:1613` is **not a label** - it is `sb.AppendLine` feeding the read-only inspector `TextBox`, rewritten per mouse move, and moving it behind an (i) would delete a feature | Repo-wide sweep during item 18's recon. Each error would have mis-scoped the work it appears in: inheriting the existing `ToolTip` would have given a toolbar a 30-second `AutoPopDelay`. |
| `TODO.md` item 18.4, the docked-label list | Eight labels plus `MapEditorPanel.cs:1613`, described as "roughly a dozen" | Thirteen, once `:1613` is struck and the four the list missed are added: `ParticlePreviewPanel.notice`, `FontEditorPanel.glyphNote` and `.previewNote`, and `Editor.Designer.cs`'s `ViewerLimitsLabel`. `ClientScriptEditorPanel` holds **two** where the list cites one line, so the single citation there undercounts as well | Swept the production project for a `Label` carrying a fixed multi-sentence explanation that no code path ever reassigns. The reassignment test is what separates the two families: every `header`, every `status`, `ConfigEditorPanel.notes`, `FontEditorPanel.kerningNote`, `EntityBrowserPanel.noticeLabel` and both `previewNote`s in the LoadingSprites and WorldMap tabs read as paragraphs and are rewritten on selection, so they stay labels. |
| `reference/index-architect-03.md`, and production XML doc comments quoting it | Component and interface counts, unqualified | Every figure in that document is the **repack's**. It never names a cache. The vanilla b639 capture holds 40,883 components across 1,067 interfaces against the repack's 42,256 across 1,078 | Measured both ways by `RealCacheInterfaceLayoutTests`. The same unqualified figures were copied into `InterfaceComponentDefinition.cs` doc comments, where they read as properties of the format. |

## Corrected during the interface layout work

| Where | Was | Is | How it was settled |
|---|---|---|---|
| The 26a design spec, and any test written from it | `(-25945 * 765) >> 14` equals **-1211** | It equals **-1212**. `-1211` is what a truncating `/ 16384` produces | Arithmetic, checked independently by three reviewers and then by the test itself. This is the sharpest example in the repo of the trap `CLAUDE.md` already records: the expected value **was** the defect's output, so a test written verbatim would have passed against the wrong implementation, failed against the right one, and pushed an implementer to replace the shift with a division to make it green. |
| The 26a design spec | No component in either cache is its own parent, so a cycle test can assert emptiness | **Group 468 file 1 stores its own file id as its parent**, byte-identically in both caches. Exactly one cyclic component and zero dangling, in each | A reviewer read the raw header bytes; `RealCacheInterfaceLayoutTests.ParentCyclesExistInThisCacheAndAreSurvived` then confirmed it independently against both caches. The resolver is cycle-proof by construction as a result, rather than by an assertion that would have failed on its first run. |
| The 26a design spec, clip rule | One exception, `ComponentType == 2` | **Two.** Type 9, a line, also extends its right and bottom by one pixel before the clip intersection (`Node_Sub10_Sub24.java:197-200`), because a line's endpoint is inclusive | Corroborated by the independently written hit-test pass at `client.java:733-736`. Both caches hold 367 line components, and the omission would clip the last pixel off every one - a defect no test that does not draw can see. |
| The 26a design spec, and `index-architect-03.md`'s field table read naively | The resolved field names suggest the axes are crossed, since the byte called `widthMode` computes a value from `RSInterface.height` | The axes are **not** crossed. The obfuscated *wire* names are shifted one position - the client's `height` is the base width, its `width` is the base position Y - and this project's decoder already uses the corrected names | `Node_Sub10_Sub24.java:102-104` passes the four resolved values to `java.awt.Rectangle.setBounds(x, y, width, height)`, a JDK signature that cannot be obfuscated, plus four independent downstream uses. |
| Assumption that a sizing mode above 2 might occur | - | **Sizing modes 3 and 4 occur zero times on either axis in either cache.** Positioning modes run 0..5 and mode 5 occurs 57 times on x and 56 on y | `RealCacheInterfaceLayoutTests.LayoutModeCoverage_...`, measured on both. So the aspect-ratio cross-links are defended only by hand-written unit tests, and the positioning catch-all arm is genuinely load-bearing: a `switch` with `case 5` and no `default` would have mislaid 113 real components. |
| `index-survey/index-002-CONFIG.md` | 18 config groups have a client provider, 17 do not | 16 do, 19 do not - two of the 18 providers name groups absent from this cache | Cross-referencing each provider against the reference table's group list. |
| `CLAUDE.md` reusable-tab section | The Interfaces tab is a raw listing because index 3's format is not reverse engineered | Both halves were true when written and neither is now | Index 3 was implemented. |
| `CLAUDE.md` invariants | Four indexes carry four zero bytes per file past the end of their reference table | Repack residue. The vanilla b639 capture has no trailing bytes on any of its 35 tables | Parsing all 35 tables in both caches and comparing each against a field-by-field length. |
| `CLAUDE.md` invariants | Four indexes hold groups their reference table does not declare, with the ids listed | Repack residue. The vanilla capture has no orphan groups on any index | Same parser, comparing live idx slots against declared ids. Written into CLAUDE.md by this session, so it lasted about a day. |
| This log, first revision | The repack adds 55 item groups, roughly 14,000 items | Both caches declare 80 item groups. The repack's idx19 has 135 *slots*, 55 of them dead records pointing at sector zero. The real delta is 43 files | Dividing an idx file's size by 6 counts allocated slots, not declared groups. It also missed indexes 27 and 29, whose file counts move inside an unchanged group count. Six indexes carry content deltas, not four. |
| `index-survey/index-032-LOADING-SPRITES.md` | Index 32's JPEGs have a fourth component that is "min 0 max 0 across all 21 files" | The raw sample value is **255**, not 0. PIL and libjpeg report a marker-less four-component JPEG through an inverted CMYK rawmode, so every value read that way is `255 - raw`; the give-away is that the same reading puts the chroma planes at 127 where a decoder that level-shifts by the specification puts them at exactly 128. The conclusion is unchanged - the plane is constant in all 21 files and in the client's own probe blob, so it carries nothing - but the number was an artefact of the tool, not a fact about the file | Decoding the entropy stream directly rather than through a library, and checking the result against libjpeg's planes with and without the inversion: inverted, luma agrees to within 1 and the fourth plane exactly; uninverted, both are 255 out |
| `index-survey/index-032-LOADING-SPRITES.md` | The YCbCr reading of components 0-2 is a gap to be closed, with rendering as the evidence | It is settled by the file's own tables, and rendering is not evidence. The two quantisation tables are the Annex K luminance and chrominance tables at IJG quality 75, assigned to components 1/4 and 2/3 respectively; only 2 and 3 are subsampled; and the client's capability probe `Class74.aByteArray546` carries the identical two quantisation tables and four Huffman tables, so the client is stating what an index-32 image is | `LoadingSpriteCodecTests` derives the expected tables from the specification and `RealCacheLoadingSpriteTests` compares all 21 cache images against the client's blob |
| `LoadingSpriteJpegPolicy.cs` remarks, and the row above by implication | Whether the client's AWT path draws these files the way we do is "untested and untestable from this side", so the file's own tables are the only thing that can settle the colour model | It is directly testable and the test agrees with us. The client's path is `Toolkit.createImage`, `MediaTracker.waitForAll`, `PixelGrabber` (`Class271.java:29-65`), so the question is only what a JVM does with the bytes - and a JVM can simply be asked. Under **JDK 8**, all 21 payloads of the vanilla capture decode and `PixelGrabber`'s pixels match `JpegRaster.ToArgb` on **1,149,294 of 1,176,093 pixels (97.72%)**, never differing by more than **3** levels on any channel, mean absolute channel error **0.019**; the repack's 23 give 97.76% and the same worst delta. The CMYK reading libjpeg defaults to for a marker-less four-component file sits **53.84** levels away per channel and matches **4** pixels of 1,176,093, so the comparison discriminates and the agreement is a result rather than a coincidence. The residue is libjpeg's inverse DCT and chroma upsampling against ours, not a colour model. **The editor's colour path is what the client drew, and the policy's refusal is not built on a wrong model.** | `tools/loading-sprite-jvm-check/`: `index32.py extract` reads idx32 and the dat2 raw, `ClientImagePath.java` replays `Class271.method3277`, `OurReading` dumps the production path, `index32.py compare` scores them. Independently, `gunzip(Class74.aByteArray546)` transcribed straight from `Class74.java:6-33` is byte-identical to `LoadingSpriteCodecTests.ProbeHex` (sha1 `50d1e626...`), and our reading of the probe equals JDK 8's exactly |
| `LoadingSpriteJpegPolicy.cs`, which treats the probe as evidence the client can decode these files | Left the impression that the probe passes | It passes only on JVMs of the client's era. Under **JDK 11** `Toolkit.createImage` raises `sun.awt.image.ImageFormatException: Unsupported color conversion request` from the native `JPEGImageDecoder.readImage` for every payload in both caches **and for the client's own probe blob**, so `Class116.method2162` catches it and returns false, `Node_Sub5_Sub2.aBoolean5535` becomes true, and `InterfaceSettings.java:72-74` opens **index 34** rather than 32. Isolated to the component count: a three-component JPEG decodes on both JDKs, and a four-component one carrying an Adobe APP14 CMYK marker is refused by both. So the probe is exactly the canary it looks like, and the measurement above states what the client drew in 2011, not what a client on a current JVM would draw | The same harness run under both JDKs, plus two synthetic control images to separate "four components" from "Toolkit is broken" |
| `JpegRaster.IsConstant` remark | The flat fourth plane is what licenses discarding it, so a varying one would be information we must not drop | The JVM drops the fourth plane either way. The repack's undeclared orphan group **498** (348x114) has a genuinely varying fourth plane, and JDK 8 still returns the planes-0-2 YCbCr reading of it, within **2** levels on every channel across 39,672 pixels. `ToArgb` refusing a varying plane is therefore this editor choosing to stop rather than silently drop something, and is deliberately **stricter** than the client. Behaviour left unchanged; only the justification was wrong | Same harness, with the refusal bypassed for that one file purely to ask what the JVM does with it |
| `index-survey/index-015-SFX3.md` trap 1 | Bit 1 of an index-15 sample reference is "read and thrown away" by the client | It is the sustain flag. `Node_Sub44.java:217` folds it into the tuning word as `0x8000 & ((v-1) << 14)` and `Node_Sub31_Sub2.java:994,997` reads it back as "is the tuning negative", giving the voice a remaining-ticks counter of -1. The trap's advice - keep the raw reference - was right for a weaker reason than the one that actually applies | Following the constructor to the end rather than stopping at `method1517`, which is where the trap was written from. The field's two halves are 260 lines apart in the same file. |
| `index-survey/index-015-SFX3.md` field list, `AGENTS.md` index map | Index 15 is "176 groups of ~290 bytes, each a sparse 256-entry table"; `aByteArray4247` is filter/cutoff; `anInt4249` is a trailing count | A patch is 128 keys, not 256 (`Node_Sub44.java:106-112`); the payload is 57,708 bytes over the 176, 273 to 1018 with a mean of 327.9; `aByteArray4247` is pan (`Node_Sub31_Sub2.java:979,1109-1118`) and `anInt4249` the patch volume (`:977`) | Decoding all 176 patches through the C# codec, and reading what the client does with each field rather than what the survey's field list named it. Everything else the survey measured - the id block, the 6 volume curves, the 10 pan curves, the envelope histogram 164/2/1/1/7/1 - was reproduced exactly by an independent port, which is why only these rows moved. |
| `AGENTS.md` revision section | Reference-table versions identify which indexes a server customised | Index 3 carries the same version in both caches while holding 1,373 more files in the repack, and index 18 has identical group and file counts with a different version and 115 more payload bytes. A version match is not evidence an index is untouched | Comparing versions and payloads table by table across both caches. |

| `index-survey/index-009-TEXTURES.md` trap 3, and `Texture.cs`'s own comment | Node type 12 swallows opcodes "2 and 4", carried by two graphs | The set is **2, 4, 5 and 6**, two graphs each, identically in the vanilla b639 capture and in the repack. The conclusion the trap draws is unchanged and still matters - every one of them reads no bytes, so consumption balances while an encoder built from decoded state alone drops them - but the set was half the size it should have been | The opcode census in `RealCacheTextureGraphTests`, which prints every opcode whose payload is zero bytes wide, run against both caches. Worth recording for how it was nearly missed: a later pass checked the claim against the decoder's comment and the survey, found them agreeing with each other, and "corrected" the right figure to the wrong one. Both were copies of the same unmeasured sentence. |
| `index-survey/index-027-CONFIG-PARTICLES.md:9`, and the on-disk shape it lists below that | Group 0 holds 403 emitters (ids 0..402) and group 1 holds 18 effectors (ids 0..17), 421 files in total | Repack figures. The vanilla b639 capture declares **269 emitters and 16 effectors**, 285 files. Every structural claim in that survey stands - two groups, one record per file, no name hashes, the opcode tables, group 0 GZip with a two-byte version trailer - and only the populations are one cache's. The 1684 trailing zero bytes it records are the same residue `AGENTS.md` already scopes to the repack, at four bytes per file over 421 files; the vanilla table consumes to the byte. The survey's derived claim that emitter opcodes 9 and 10 reference exactly `{0..17}` cannot hold in the vanilla capture either, since only 16 effectors exist there | `RealCacheParticleTests.TheIndexDeclaresTheTwoGroupsTheClientReads` prints the declared file count of each group from the reference table rather than from a decode, run against both caches: `index 27 declares groups 0, 1; 269 emitters and 16 effectors` against `403 emitters and 18 effectors`. |

| `index-survey/index-030-NATIVE-LIBRARIES.md` gap 21 | `ReferenceTableCodec`'s whirlpool write "is not proven byte-identical anywhere", and index 30 is the sole table that can prove it | The second half stands; the first is wrong. `RealCacheConformanceTests.ReferenceTables_ReEncodeToTheCapturedBytes` iterates every table in idx255 and byte-compares each against its captured payload, and index 30 is one of them, so the 64-byte-per-group write has been covered all along. What was defended by nothing is the **recompute** in `RSCache.WriteFile:307-310`, which needs a write to reach and which no test performed on any index. The gap's advice - build a sweep here - was right for a different reason than the one it gave | Reading the conformance sweep rather than the shape tests. `RealCacheNativeLibraryTests` now names the codec branch separately anyway, because a regression in it would otherwise be one failure among thirty-five with nothing saying which branch was lost, and `RealCacheWhirlpoolWriteTests` covers the recompute. |
| `index-survey/index-030-NATIVE-LIBRARIES.md` gap 22, and `00-WORKLIST.md` section 4.9 | Raw extract and import "has to be built from scratch, not copied", there being no `SaveFileDialog`, `OpenFileDialog` or `File.WriteAllBytes` anywhere in `Editor.cs` | The literal claim about `Editor.cs` is true and reads as a claim about the editor, which it is not. `LoadingSpriteEditorPanel` already had all three plus `File.ReadAllBytes`, including the unchanged-bytes check that is the non-obvious half, and is the shape `CachePayloadTransfer` generalises | Grepping the whole production project rather than `Editor.cs`. A scoped claim that generalises silently is the same failure as a stale one. |

## Corrected during the interface structural-edit work

| Where | Was | Is | How it was settled |
|---|---|---|---|
| `InterfaceEditorPanel.InterfaceListing.IdRange`'s own comment, against `InterfaceComponentEdits`' type remarks | "Index 3's groups are sparse: the count and the highest id disagree, so a reader who assumed 0..count-1 would ask for components that do not exist" | **Every declared group in both caches numbers its components 0 to n-1 with no hole**: 1,067 groups over 40,883 files in the vanilla b639 capture, 1,078 over 42,256 in the repack. The planner's opposite claim was the right one | `RealCacheInterfaceStructureTests.EveryInterface_NumbersItsComponentsDenselyFromZero`, read from the reference table rather than from a decode, run against both. **Two of this project's own files stated opposite facts about the same index**, and the renumbering every structural edit performs exists only if the planner's reading is right - so this had to be measured before a line of the applier could be written, not chosen. |
| The assumption, implicit in planning a delete and an insert as opposites, that `PlanInsert` undoes `PlanDelete` | An insert at the position a component was deleted from restores it | Only when the insert lands on the **id** that was vacated. `PlanInsert` is stated in sibling positions and yields whatever id the sibling standing at that position currently holds, so a parent whose remaining children are not contiguous with the deleted one cannot express the inverse at all, and a parent left with no children appends to the end instead | The first draft of `DeletingAnInteriorLeafAndPuttingItBack_LandsOnTheOriginalStoredBytes` asserted the inverse and failed: it deleted the only child of its parent, and the insert appended to the end of a 21-component group instead of restoring id 0. Worth recording rather than quietly working around, because an undo stack built on the planner will meet the same limit. |

## Reference material worth pulling in

| Topic | Source | Why |
|---|---|---|
| Index 12 CLIENT_SCRIPTS | **RuneStar** (GitHub) | Carries clientscript opcode definitions and decompilation work. Index 12's codec is small, but the worklist notes a *useful* tab needs a disassembler over roughly 580 opcodes across three client dispatchers, and that is the part worth not reinventing. Check what it covers for build 639 specifically before relying on it - it is oriented at later revisions. |
